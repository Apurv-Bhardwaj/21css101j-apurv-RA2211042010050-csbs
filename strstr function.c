#include<stdio.h>
#include<string.h>
void main()
{
char str1[30], str2 [30];
  char *found;
  printf("Enter two strings:\n");
  gets(str1);
  gets(str2);
  printf("string2 is found a tposition %d in string1\n", found-str1);
}
