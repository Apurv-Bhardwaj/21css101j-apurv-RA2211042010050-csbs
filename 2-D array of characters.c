#include <stdio.h>
main()
{
char pet [2][5] = {"Lion","Cat"};
  int i,j;
  for(i=0; i<2; i++)
  {
  printf("\n%d", (pet+i)+j);
    j=0;
    while (*(*(pet + i)j) != '\0')
  }
  printf("%c", *(*(pet + i) + j) );
  j++;
}
}
  }
